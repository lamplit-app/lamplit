{
  "name": "server",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "description": "Lamplit persistence server: JSON documents on disk, and the built app in front of them.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=20.19"
  },
  "dependencies": {
    "express": "^5.2.1",
    "qrcode": "^1.5.4"
  }
}
