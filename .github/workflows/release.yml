name: Release

# Pushing a tag is the release.
#
#   npm version patch     bumps package.json, commits, tags v0.1.1
#   git push --follow-tags
#
# Everything below then happens on its own, and stops one click short of done:
# one job makes a *draft* release and puts the changelog's top section on it,
# both platforms build and upload into that one draft, and a person publishes
# after looking at what arrived. Nothing is signed (see PLAN.md §5.5), so what
# is worth looking at is that both platforms produced what they should.
#
# `workflow_dispatch` runs the same build without publishing anything, and
# leaves the installers and the zip as workflow artifacts. That is how to prove
# a change to this file before trusting it with a tag.

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

env:
  # electron-builder downloads Electron itself; a cache would be a second thing
  # that could be stale, for a download that happens twice a release.
  ELECTRON_CACHE: ${{ github.workspace }}/.cache/electron

jobs:
  # The draft, made once, before anything can upload to it.
  #
  # Both installer legs run `electron-builder --publish always`, and that
  # creates the release for the tag when it cannot find one. Run at the same
  # moment, neither can: each listed the releases before the other had made
  # anything, so v0.2.0 got *two* drafts with half the release in each — the
  # Windows installers and the notes on one, the AppImage and the .deb on the
  # other. Both jobs were green. electron-builder does find a draft that
  # already exists, which is why the first release, where the two platforms
  # never published from the same run, looked fine.
  #
  # The notes are taken here rather than in a job at the end for the same
  # reason: `gh release edit <tag>` cannot say which of two drafts it means.
  draft:
    name: Draft release
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # The section at the top is about to become this release's notes, so it
      # had better be this release's section. Between releases it is
      # `## Unreleased`, which is when tags get pushed — and the website drops
      # that section on purpose, so the release would have gone out under a
      # heading saying it had not, with no notes page to link to.
      - name: Check the changelog names this version
        run: npm run check:notes -- --version "${GITHUB_REF_NAME}"

      # The changelog is written by hand and is the release notes; this only
      # takes the top section of it, so there is one place to write them.
      - name: Take the top section of CHANGELOG.md
        run: |
          awk '/^## /{ if (seen++) exit } seen' CHANGELOG.md > notes.md
          test -s notes.md || { echo "CHANGELOG.md has no section to publish"; exit 1; }
          cat notes.md

      - name: Create the draft
        run: gh release create "${GITHUB_REF_NAME}" --draft --title "${GITHUB_REF_NAME#v}" --notes-file notes.md
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  installers:
    name: ${{ matrix.os }}
    # Off a tag, the draft is waiting before either platform builds. A
    # dispatched run has no draft job to wait for and skips this: `always()`
    # keeps the matrix running when `draft` was skipped, and the `result`
    # check still fails the build if it ran and failed.
    needs: draft
    if: always() && (needs.draft.result == 'success' || needs.draft.result == 'skipped')
    runs-on: ${{ matrix.os }}
    strategy:
      # One platform failing should not hide what the other would have said.
      fail-fast: false
      matrix:
        # No macos-latest: an unsigned macOS build is not something this project
        # would publish, so there is nothing for that runner to make. PLAN.md
        # §5.5 has the reasoning and what a contributor with the licence needs.
        os: [windows-latest, ubuntu-latest]

    steps:
      - uses: actions/checkout@v7

      # Install, lint, formatting, the docs and release-notes checks, unit
      # tests, the build, a packaged copy started and asked for /api/health,
      # and the end-to-end suite — the same action ci.yml runs on every push,
      # so a tag cannot pass a check that main would fail. On
      # both platforms here, because the installers are made from what it
      # leaves behind.
      - uses: ./.github/actions/verify

      # Off a tag the build says `--publish never` for itself: electron-builder
      # defaults to "on tag or draft" when it detects CI, and a draft release
      # waiting to be looked at is exactly what a dispatched run would have
      # overwritten with a build of main.
      - name: Build the installers
        run: node tools/desktop.mjs --dist ${{ startsWith(github.ref, 'refs/tags/v') && '--publish' || '' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # The third way in, for a machine that has Node and would rather have one
      # megabyte than a hundred — and the only way in on a Mac. It is zipped
      # from the folder the installers were just built from, so the two channels
      # are the same server, the same app and the same dependencies by
      # construction rather than by promise.
      #
      # Linux only: one zip is enough, and it has to be made on a Unix runner —
      # the archive carries the file modes of the machine that wrote it, and
      # start.sh has to arrive runnable.
      - name: Build the zip
        if: runner.os == 'Linux'
        run: |
          node tools/package.mjs --stage build/desktop-stage --zip-only
          mv build/lamplit-*.zip build/Lamplit.zip

      # Unversioned, like the installers, so that
      # releases/latest/download/Lamplit.zip stays a link forever. The version
      # is inside, in package.json and README.txt.
      - name: Put the zip on the draft release
        if: ${{ runner.os == 'Linux' && startsWith(github.ref, 'refs/tags/v') }}
        run: gh release upload "${GITHUB_REF_NAME}" build/Lamplit.zip --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Keep the installers when nothing was published
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        uses: actions/upload-artifact@v7
        with:
          name: installers-${{ matrix.os }}
          path: |
            build/desktop/*.exe
            build/desktop/*.AppImage
            build/desktop/*.deb
          if-no-files-found: error

      - name: Keep the zip when nothing was published
        if: ${{ runner.os == 'Linux' && !startsWith(github.ref, 'refs/tags/v') }}
        uses: actions/upload-artifact@v7
        with:
          name: zip
          path: build/Lamplit.zip
          if-no-files-found: error
